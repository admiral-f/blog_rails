<h1><%= @article.title %></h1>
</br>
<p>
	<%= @article.text %>
<p>
<hr>
<% @article.comments.each do |comment| %>	
	<p><b><%= comment.author %>: </b>
		<%= comment.body %>
	</p>
	</br>	
<% end %>
<hr>
<%= form_for([@article, @article.comments.build]) do |f|  %>
	<p>
		<%= f.label :author %> </br>
		<%= f.text_field :author %>		
	</p>
	<p>
		<%= f.label :body %> </br>
		<%= f.text_area :body %>		
	</p>
	<p>
		<%= f.submit %>
	</p>
<% end %>
